你現在是一名專精於教室觸控大螢幕（電子白板）教具開發的資深前端工程師。請為我撰寫一個完全仿照課堂拔河比賽的「雙人拔河對戰遊戲」，輸出為【單一 HTML 檔案】（包含完整 CSS 與 JavaScript，點開即可離線執行）。

### 一、 畫面佈局（左右對稱，適合大螢幕左右站人操作）：
1. **整體版面**：全螢幕三欄式設計。
2. **左側（Team 1 藍隊作答區）**：
   - 頂部：隊名「Team 1」與當前得分。
   - 中間：題目顯示框（字體超大，例：`6 × 10 = ?`）與答案輸入顯示條。
   - 底部：內建虛擬觸控數字鍵盤（包含 0~9 數字按鈕、清空鍵 C、送出確認鍵 ✓），方便學生直接在觸控螢幕上按鍵作答。
3. **中間（拔河動畫區）**：
   - 頂部：計時器與目前拉扯狀態。
   - 中央場地：垂直虛線中線（判斷線），一條水平繩子與中央紅色彩帶。
   - 拔河隊員：繩子兩側各有一組生動的拔河卡通小人（可用 SVG 或 CSS 繪製，呈現傾斜用力的拔河姿態）。
4. **右側（Team 2 紅隊作答區）**：
   - 與左側結構完全相同的獨立題目區與數字鍵盤。

### 二、 核心互動與規則機制：
1. **拔河拉扯**：
   - 起始狀態繩子紅帶位於中心虛線。
   - 哪一隊率先輸入正確答案並按下「✓」，拔河隊伍就往該方向位移一段距離（例如平移 40px），並立即刷新該隊下一題。
   - 輸入錯誤時，輸入框出現紅色震動警告，答案清空但小人不移動。
   - 當某一隊將繩子拉過設定的決勝線（例如領先達 5 題距離），遊戲結束，跳出該隊獲勝的慶祝動畫與「再戰一局」按鈕。
2. **自訂題庫設定（最重要）**：
   - 請在 `<script>` 最上方建立結構一目了然的 `const QUESTION_BANK = [...]`。
   - 每個題目包含 `question`（題目文字）和 `answer`（正確答案數字或代號）。
   - 請在程式碼中用清楚的中文註解，詳細說明老師如何快速貼上自己單元的題目與答案。

請提供完整、無縮減、可直接存成 .html 檔執行的程式碼。