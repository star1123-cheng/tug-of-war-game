# 大電視觸控雙人拔河對戰遊戲 — 題庫替換與課堂操作說明手冊

本遊戲專為教室大型觸控電視／電子白板（65 ~ 86 吋，如 BenQ、ViewSonic、Promethean、Newline 等）與雙人競賽設計。學生分站大螢幕左右兩端進行搶答，具備「防止原生虛擬鍵盤跳出干擾」、「雙人多點觸控零延遲」、「Web Audio 離線音效」與「生動拔河拉扯動畫」。

---

## 一、 快速開始與大螢幕操作訣竅

1. **直接開啟**：
   * 在教室電腦或大電視連線的電腦上，使用 **Google Chrome** 或 **Microsoft Edge** 瀏覽器，按兩下打開 `拔河遊戲.html` 即可立即離線遊玩（無須連外網）。
2. **全螢幕沉浸投影**：
   * 點擊右上角的 **「⛶ 全螢幕」** 按鈕，或按下鍵盤上的 **`F11`** 鍵進入全螢幕模式，隱藏瀏覽器分頁標籤，讓畫面更專業震撼。
3. **觸控設計細節**：
   * 答題區採用自製大型觸控數字鍵盤，**點擊輸入時絕對不會彈出 Windows 或 Android 系統的原生虛擬鍵盤**，確保比賽畫面視野完整。
   * 左右兩側完全獨立運作，支援兩位學生同時狂按觸控螢幕，互不干擾。

---

## 二、 如何替換自訂題目（雙軌替換機制）

我們提供兩種替換題目的方式，您可以依據備課時間與使用情境自由選擇：

### 方法一：課堂現場直接替換（🌟 最推薦，免開程式碼、10 秒生效）

當您已經在教室大螢幕前，可以直接利用遊戲內建的設定視窗修改：

1. 點擊螢幕左上角的 **「⚙️ 題庫設定」** 按鈕。
2. 彈出視窗後，您可以在文字框中直接編輯或貼上題目。
3. **格式規則**：每行一題，中間以逗號隔開：
   ```text
   題目, 答案
   ```
   > 💡 **貼心防呆**：支援半形逗號 `,` 或中文全形逗號 `，`。
4. **快速套用範本**：
   * 視窗上方提供四顆快捷按鈕：【🔢 高年級數學四則】、【📐 幾何與度量衡】、【📚 國語數字成語】、【🧪 自然科學趣味題】，點擊即可一鍵帶入。
5. **設定勝負步數**：
   * 可下拉選擇「決勝拉力步數」（預設淨領先 5 題獲勝，亦可調整為 3、7 或 10 題）。
6. **點擊「儲存並重啟遊戲」**：
   * 系統會自動將題庫儲存於大電視瀏覽器的 `localStorage`（本地快取），即使按重新整理，自訂題目也不會消失！
   * 若未來想還原，隨時點擊視窗內的 **「恢復預設題庫」** 即可。

---

### 方法二：直接修改 HTML 原始碼（適合事前備課、永久打包）

如果您想要將客製化題目永久存檔，並將 `.html` 檔案寄送給其他班級老師使用：

1. 在 `拔河遊戲.html` 檔案上按滑鼠右鍵，選擇 **「使用記事本開啟」**（Notepad）或 VS Code 等文字編輯器。
2. 按 `Ctrl + F` 搜尋關鍵字：`DEFAULT_QUESTION_BANK`（大約在第 510 行）。
3. 您會看到以下清晰的題目陣列：
   ```javascript
   const DEFAULT_QUESTION_BANK = [
     { question: "8 × 9 = ?", answer: "72" },
     { question: "125 ÷ 5 = ?", answer: "25" },
     { question: "一個三角形的內角和是幾度？", answer: "180" },
     { question: "時針走一圈是幾小時？", answer: "12" }
   ];
   ```
4. 依樣畫葫蘆增加或替換您需要的題目與答案：
   * `question`：引號內放題目文字。
   * `answer`：引號內放標準答案（數字或英文皆可）。
5. 存檔後重新打開網頁，即可看到更新後的題目。

---

## 三、 各學科題庫範例參考（可直接複製貼上）

您可以直接複製以下內容，貼入「⚙️ 題庫設定」的文字框中：

### 範例 1：數學科 — 質數與倍數
```text
最小的質數是？, 2
二位數中最大的質數是？, 97
10 以內有幾個質數？, 4
12 和 18 的最大公因數是？, 6
4 和 6 的最小公倍數是？, 12
1 到 100 之間有幾個質數？, 25
```

### 範例 2：國語科 — 成語數字填空
```text
「( )面楚歌」應填入？, 4
「( )上( )下」形容忐忑不安？, 78
「( )言( )鼎」比喻說話有份量？, 19
「垂涎( )尺」應填入？, 3
「五顏( )色」應填入？, 6
「接二連( )」應填入？, 3
「( )顧茅廬」劉備請孔明？, 3
「( )牛( )毛」比喻極少數？, 91
```

### 範例 3：自然科學 — 生活科學常識
```text
成人全身骨骼大約有幾塊？, 206
常溫常壓下水沸騰是幾度C？, 100
水在幾度C時結冰？, 0
昆蟲身體分為幾大部分？, 3
昆蟲有幾隻腳？, 6
蜘蛛有幾隻腳？, 8
太陽系目前公認有幾大行星？, 8
人體心臟有幾個心室與心房總和？, 4
```

---

## 四、 競賽規則與課堂帶動建議

1. **競賽機制**：
   * 起始繩索中央紅帶對齊黃色中線。
   * 學生任一隊按「✓」答對後：該隊拔河小人猛烈向後拉扯，繩子平移一格（40 px），並立即換下一題。
   * 若輸入錯誤並按「✓」：輸入框會產生紅色震動警示並自動清空，小人不移動，學生可即時重新計算再次輸入。
   * 當某一隊淨勝題數達到設定門檻（預設領先 5 題），觸發全螢幕勝利彩紙彩帶動畫與歡呼號角音效！
2. **分組對抗建議**：
   * 可以讓全班分成兩大組（藍隊與紅隊），每題派一位選手上台觸控作答，答對後接力換下一位同學，進行班級全員接力賽，課堂氣氛極佳！
